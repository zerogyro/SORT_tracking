from sort import  *


if __name__ == '__main__':
    # mot_tracker = Sort()
    # detections = [[477.417,133.575,60.972,206.672,0.985314],[387.83,158.467,100.509,182.139,0.97094]]
    # dets = np.asarray(detections)
    # dets[:, 2:4] += dets[:, 0:2]
    # print(dets)
    # track_bbs_ids = mot_tracker.update(dets)
    # print(track_bbs_ids)
    # for d in track_bbs_ids:
    #     print('%d,%.2f,%.2f,%.2f,%.2f,1,-1,-1,-1' % (d[4], d[0], d[1], d[2] - d[0], d[3] - d[1]))

    sln = np.empty((0,5))
    k = sln.predict()
    print(sln)